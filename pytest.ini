[pytest]
markers =
    integration: tests that require live external services (LLM API, database). Run with: pytest -m integration
    llm: tests that call the real configured LLM (and often live tools/DB). Run with: pytest -m llm
# Default unit run excludes live LLM and integration suites
addopts = -m "not llm and not integration"